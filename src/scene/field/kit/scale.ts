/**
 * Единый коэффициент габаритов промыслового оборудования.
 *
 * ЧТО МАСШТАБИРУЕТСЯ. Только размеры самих установок, относительно их точки
 * посадки. Координаты не трогаются вообще: ГЗУ-25 остаётся ровно там, где на
 * плане написано «ГЗУ-25», трасса идёт по фактической съёмке, устье скважины —
 * по реестру фонда. Схема промысла от коэффициента не зависит.
 *
 * ЗАЧЕМ. Настоящие габариты на участке 5352 × 4682 м честны, но невыразительны:
 * станок-качалка длиной девять метров — это 0,17 % ширины кадра на обзорном
 * плане. Для показа установки имеет смысл укрупнить, сохранив расстановку.
 *
 * ПОЧЕМУ ОДИН КОЭФФИЦИЕНТ, А НЕ ПОШТУЧНО. В ТЗ §2 есть прямая претензия к
 * референсному прототипу: там объекты подгонялись индивидуальными
 * `scale.setScalar(1.3…1.6)`, и результат владелец продукта назвал «слишком
 * мелко/крупно». Одно явное число вместо догадок по каждому объекту — это и
 * есть дисциплинированная версия того же приёма.
 *
 * ЧТО ОБЯЗАНО МАСШТАБИРОВАТЬСЯ ЗАОДНО, иначе сцена развалится:
 *  · высота подвеса проводов — вместе с опорами ВЛ, иначе провода отвяжутся
 *    от траверс и повиснут сами по себе;
 *  · высота надземной трубы — вместе с её опорами, по той же причине;
 *  · радиусы наведения камеры в `focus.ts` — иначе при клике камера подойдёт
 *    к укрупнённой качалке вплотную и та не поместится в кадр.
 *
 * ГДЕ ПРЕДЕЛ. Упирается в плотность расстановки. Кусты стоят в среднем через
 * 350 м — запас большой. Опоры ВЛ идут через 40–80 м, и при коэффициенте
 * больше трёх соседние начнут задевать друг друга траверсами. Резервуары на
 * промысловом комплексе стоят вплотную — там предел ещё ниже.
 *
 * Значение 1 — фактические габариты. Настраивать имеет смысл после того, как
 * все объекты фазы A стоят: иначе настраивать придётся дважды.
 */
export const EQUIPMENT_SCALE = 1;
