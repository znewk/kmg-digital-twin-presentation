/**
 * Сколько кадра снизу занято плашкой — доля высоты экрана.
 *
 * Нужна камере: на этапах с нижней плашкой предмет должен вставать в центр
 * СВОБОДНОЙ полосы, а не экрана. Раньше эта доля была вписана в ракурс числом,
 * подобранным по одному монитору, и на другом разрешении разъезжалась.
 *
 * Почему она не постоянна сама по себе. Высота плашки определяется
 * содержимым, а содержимое переносится по-разному в зависимости от ширины:
 * на широком экране плитки ИТ-ландшафта укладываются в меньшее число строк, и
 * плашка становится ниже. Плюс общий коэффициент интерфейса упирается в
 * потолок на больших разрешениях, из-за чего слой в логических пикселях
 * оказывается шире базовой раскладки. Итог: доля меняется, и предсказать её
 * формулой нельзя — только измерить.
 *
 * Значение живёт в мутабельной ссылке, а не в сторе: его пишет наблюдатель
 * размера при каждой перекомпоновке, а читает кадровый цикл. Через состояние
 * React это дало бы перерисовку на каждый пиксель протяжки окна.
 */
export const panelReserve = { current: 0 };
