# Справочник наименований систем

Полный перечень названий программных продуктов, встречающихся в проекте, с
указанием точных мест в коде.

Дата составления: 12.08.2026, коммит `70e33e4`.

---

## ⚠ ПЕРЕХОД НА ТЕРМИНОЛОГИЮ ABAI ВЫПОЛНЕН

Заказчик утвердил единую раскладку, и проект на неё переведён. **Разделы 1 и 5
ниже описывают состояние ДО перехода** и оставлены как история: по ним видно,
что именно менялось.

Что сделано:

- раскладка по умолчанию — `abai` (`useShow.ts`); переключатель оставлен в коде
  и доступен через `?naming=`, но состояние при старте одно;
- группа модулей ЦД переименована: «Модули ЦД», пояснение — «Модули цифрового
  двойника, закрывающие функциональные разрывы действующего контура ABAI»;
- восемь фраз с именем внутри предложения переписаны с согласованием;
- две подписи в панелях слоя данных переведены на «КХД».

**Побочный эффект, который пришлось развести отдельно.** После перехода
действующие модули ABAI «Цифровое бурение» и «Цифровой мониторинг ТКРС»
называются так же, как внедряемые пилотом контуры, — таково утверждённое
соответствие. Но состояние у них противоположное: действующие числятся и не
работают, внедряемые в пилоте. Действующим добавлено уточнение
**«(действующий)»**: без него на экране оказались бы две одинаковые подписи с
разными статусами, и пометка «не функционирует» могла прочитаться как
относящаяся к пилоту, то есть ровно наоборот.

Маркировка зрелости по §7 сохранена в полном объёме: ЦРНС и УЗ — «новое для
ЭМГ», действующие ЦБ и ТКРС — «не функционирует», AI-слой — «целевой контур»,
эксплуатационные показатели — иллюстративные.

Вендорские имена остались только в поле `nameNedra` (не выводится при
раскладке `abai`), в именах констант кода и в комментариях.

---

## Как устроены имена в проекте

Наименования живут в **трёх независимых местах**, и заменять нужно во всех трёх:

| Где | Что там | Файл |
|---|---|---|
| **Реестр модулей** | Продукты ЦД: по два имени у каждого — вендорское и в терминах ABAI | `src/data/modules.ts` |
| **ИТ-ландшафт** | 80 упоминаний существующих систем ДЗО по этапам процесса | `src/data/upstreamData.ts` |
| **Тексты** | Имена, вписанные в предложения — досье модулей, описания шагов, реплики этапов | россыпь, перечислено ниже |

У модулей реестра предусмотрен **переключатель раскладки** (`naming`:
`nedra` / `abai` / `hybrid`, ТЗ §5) — то есть два имени показываются
поочерёдно и уже сейчас заменяются вместе. У систем ИТ-ландшафта и у имён,
вписанных в текст, такого механизма нет: там простые строки.

---

## 1. Модули цифрового двойника — ПОДЛЕЖАТ ЗАМЕНЕ

Файл: `src/data/modules.ts`, объект `RAW`.
У каждого два поля: `nameNedra` (вендорское) и `nameAbai` (в терминах ABAI).

| Ключ | `nameNedra` — заменить | `nameAbai` — сейчас | Строка |
|---|---|---|---|
| `numex` | **Nedra.NUMEX** | ABAI ЦРНС 2.0 | 189 |
| `numexOptimize` | **Nedra.NUMEX Optimize** | ABAI Управление заводнением 2.0 | 198 |
| `rtm` | **Nedra.RTM** | ABAI Цифровое бурение | 206 |
| `wwo` | **Nedra.WWO** | ABAI Цифровой мониторинг ТКРС | 214 |
| `digitalTwin` | **Nedra.DIGITAL TWIN** | ABAI Планирование добычи и мониторинг 2.0 | 223 |
| `digitalTwinPipe` | **Nedra.DIGITAL TWIN · Трубопроводы** | ABAI ПДиМ (Целостность трубопроводов) 2.0 | 232 |
| `infraplan` | **Nedra.INFRAPLAN** | ABAI Наземная инфраструктура | 241 |
| `nedraData` | **Nedra.DATA (NDP)** | КХД. Слой бизнес-интеграций | 250 |

Восемь продуктов. Плюс подпись источника:

| Что | Значение | Файл, строка |
|---|---|---|
| Метка группы | **Nedra** | `modules.ts:70` |
| Пояснение группы | «Продукты **Nedra Digital**, закрывающие разрывы ABAI» | `modules.ts:72` |

---

## 2. Модули ABAI — замене НЕ подлежат

Оставлены для полноты картины: они принадлежат заказчику.

`abaiDb` База данных · `abaiKp` Картопостроитель · `abaiTr` Технологический
режим · `abaiPdim` ПДИМ · `abaiPaegtm` ПАЭГТМ · `abaiPgno` ПГНО ·
`abaiCrns` ЦРНС · `abaiUz` УЗ · `abaiCb` Цифровое бурение ·
`abaiTkrs` Цифровой мониторинг ТКРС.

Десять модулей, `modules.ts:90–175`.

---

## 3. Внешние системы в реестре

| Ключ | Имя | Строка | Замена |
|---|---|---|---|
| `tNavigator` | t-Navigator, Petrel | 260–261 | нет — чужие продукты |
| `sapToro` | SAP ТОРО | 268–269 | нет |
| `smartField` | Интеллектуальное месторождение | — | нет |
| `aiAgents` | AI-агенты ЦД Актива | — | нет |

---

## 4. ИТ-ландшафт ДЗО — 80 упоминаний, 57 уникальных систем

Файл: `src/data/upstreamData.ts:151–201`, массив `IT_LANDSCAPE`.
Звёздочкой помечены системы заказчика (`own: true`), они подсвечиваются иначе.

**Геология и ГРР** (17): Petrel, Techlog, tNavigator, Peloton, PVTi, PVTSim,
ProSource, Kingdom, GeoGraphix, Hampson Russel, Spark 1.5, ГеоПоиск, Жулдыз,
ABP+, ABAI (БД) \*, Excel, Outlook

**Разработка** (10): Petrel, tNavigator, Eclipse, OFM, Жулдыз, ABAI (УЗ) \*,
ABAI (ТР) \*, ABAI (ПиАГТМ) \*, ABAI (БД) \*, Excel

**Бурение и ВСР** (19): Petrel, Landmark, tNavigator, Sysdrill, SoloBox,
SoloFeed, DrillSpot, Starsteer, WellView, ДЭЛ-140/150, АСРППС, ЕКПД, SAP ERP,
СЭД, WhatsApp, ABAI (БД, ПГНО, ПДИМ) \*, ABP+, Excel, Outlook

**Добыча** (17): PipeSim, UniSim, AutoCAD, Questor, СДМО, ИСУТО, ИМ,
GreenData, АСКУЭ, АСТУЭ, SCADA, SAP ERP, Procu, ABAI (БД) \*, ABP+, Excel,
Outlook

**КС и наземная инфраструктура** (17): PipeSim, UniSim, AutoCAD, Qportal.kz,
АСУ НСИ, SAP, ABC-4, SAS FM, E-Pass, eLicense, E-Qurylys, Самрук-Казына,
Procu, СЭД, ABP+, Excel, Outlook

> Это перечень СУЩЕСТВУЮЩИХ систем ДЗО из отчёта об обследовании — картина
> «лоскутного одеяла», которую цифровой двойник и должен снять. Продуктов
> вендора среди них нет, и замене они, скорее всего, не подлежат. Проверьте.

---

## 5. Имена, вписанные в текст — ОТДЕЛЬНОЕ ВНИМАНИЕ

Здесь имя стоит внутри предложения, а не в поле реестра. При замене нужно
менять и согласование в фразе, автоподстановкой не обойтись.

| Файл : строка | Фрагмент |
|---|---|
| `data/moduleDossier.ts:61` | «Сценарии разработки и опции по ключевым параметрам пласта — передаются в **Nedra.NUMEX**» |
| `data/moduleDossier.ts:88` | «Профили добычи по скважинам и кустовым площадкам — передаются в **Nedra.INFRAPLAN** для расчёта моделей инфраструктуры» |
| `data/moduleDossier.ts:138` | «Модуль ABAI Цифровое бурение в продакшене ЭМГ не используется — функцию закрывает **Nedra.RTM**» |
| `data/moduleDossier.ts:171` | «Профили добычи по скважинам и кустовым площадкам из **Nedra.NUMEX**» |
| `data/dives.ts:264` | Заголовок раздела: «Единый слой данных · **Nedra.DATA**» |
| `data/dives.ts:83` | «Инструменты — **t-Navigator и Petrel**, те же, что уже используются на пласте» |
| `data/stages.ts:322` | «ABAI закрывает учёт, **Nedra** — расчётный и модельный контур» |
| `data/fieldObjects.ts:346` | «Кандидат на бурение по результатам **NUMEX**» |

---

## 6. Служебные идентификаторы — НЕ ТРОГАТЬ

Ключи модулей (`numex`, `numexOptimize`, `digitalTwin`, `digitalTwinPipe`,
`infraplan`, `nedraData`, `rtm`, `wwo`) — это внутренние идентификаторы типа
`ModuleId`. Они встречаются в раскадровке, разделах контуров, спецификации
цепочки и досье как ссылки на реестр.

**Их менять не нужно и не следует.** Показываемое имя берётся из реестра по
ключу, а сам ключ зритель не видит никогда. Переименование ключей — это правка
шести файлов ради нулевого эффекта на экране и риск разъехавшихся ссылок.

Отдельно: `digitalTwin` встречается 63 раза именно как ключ — при поиске и
замене по строке «digitalTwin» это даст ложные срабатывания.

---

## Порядок замены, когда придёт справка

1. `src/data/modules.ts` — восемь полей `nameNedra` плюс метка и пояснение
   группы. Этого достаточно, чтобы имена сменились **везде**, где они берутся
   из реестра: бейджи модулей, панели, досье, разделы контуров, шапки экранов.
2. `src/data/moduleDossier.ts` — четыре фразы, где имя внутри предложения.
3. `data/dives.ts`, `data/stages.ts`, `data/fieldObjects.ts` — по одной фразе.
4. `src/data/upstreamData.ts` — только если меняются и системы ДЗО.

Проверка после замены: `npm run build` и поиск по репозиторию `grep -rn
"Nedra\|NUMEX\|INFRAPLAN" src` — в выдаче должны остаться только ключи
`ModuleId` и комментарии.
