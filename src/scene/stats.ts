/**
 * Показатели кадра для служебного оверлея.
 *
 * Кадровая частота сама по себе не говорит, ЧТО именно её съедает. Число
 * вызовов отрисовки и треугольников говорит: если частота просела, а вызовов
 * тысячи — виновата разбивка сцены на объекты, если вызовов сотня, а
 * треугольников миллионы — геометрия. Без этих двух чисел настройка бюджета
 * превращается в гадание.
 *
 * Пишется тем же способом, что и прогресс скролла: мутабельный объект вместо
 * состояния React. Значения меняются каждый кадр, и перерисовывать ради них
 * интерфейс шестьдесят раз в секунду недопустимо.
 */
export const renderStats = {
  calls: 0,
  triangles: 0,
  geometries: 0,
  textures: 0,
  programs: 0,
};
